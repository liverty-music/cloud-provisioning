name: liverty-music
description: Cloud resources, such as GCP, GitHub, etc. provisioned by Pulumi.
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-typescript
