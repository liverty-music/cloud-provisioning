apiVersion: v1
kind: ServiceAccount
metadata:
  name: zitadel
  annotations:
    # Workload Identity binding (GKE). The GCP service account is provisioned
    # by Pulumi (kubernetes.ts) with Cloud SQL IAM auth + Secret Manager
    # version-adder (for the bootstrap admin-sa-key upload).
    iam.gke.io/gcp-service-account: k8s-zitadel@liverty-music-dev.iam.gserviceaccount.com
