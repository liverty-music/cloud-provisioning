# Prod-only overrides for env-specific values that the base configmap.env
# currently encodes with dev placeholders. These keys merge into the
# `zitadel-config` ConfigMap via configMapGenerator.behavior: merge.
#
# Same env-leak-from-base pattern as the HTTPRoute hostname (design D1):
# base carries dev defaults; each overlay overrides per-env values.

# Public domain Zitadel advertises in OIDC discovery + the InstanceDomain
# the request Host header must match. Auth fails (HTTP 500 from the Login
# UI's SSR) if this does not match the Gateway hostname.
ZITADEL_EXTERNALDOMAIN=auth.liverty-music.app

# Cloud SQL IAM SA usernames — depend on the GCP project id. The prod
# Cloud SQL IAM user `zitadel@liverty-music-prod.iam` is provisioned by
# Pulumi as part of the prod-prereqs follow-up; the manifest declares the
# expected name so the prod ArgoCD Application syncs cleanly once the
# Pulumi resources land.
ZITADEL_DATABASE_POSTGRES_USER_USERNAME=zitadel@liverty-music-prod.iam
ZITADEL_DATABASE_POSTGRES_ADMIN_USERNAME=zitadel@liverty-music-prod.iam
