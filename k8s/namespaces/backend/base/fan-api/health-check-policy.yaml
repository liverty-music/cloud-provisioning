apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: fan-api-policy
spec:
  default:
    config:
      type: GRPC
      grpcHealthCheck:
        port: 8080
  targetRef:
    group: ""
    kind: Service
    name: fan-api-svc # Referenced via kustomizeconfig.yaml 'nameReference'
