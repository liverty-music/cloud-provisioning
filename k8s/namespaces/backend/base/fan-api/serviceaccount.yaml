apiVersion: v1
kind: ServiceAccount
metadata:
  name: fan-api
  annotations:
    # Requires the GSA created by Pulumi: fan-api@<project-id>.iam.gserviceaccount.com
    # (unify-workload-naming, cloud-provisioning #408). The concrete email is
    # bound per environment by the overlay serviceaccount patch.
    iam.gke.io/gcp-service-account: REPLACE_ME_IN_OVERLAY
