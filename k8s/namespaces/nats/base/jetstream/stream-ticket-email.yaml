apiVersion: jetstream.nats.io/v1beta2
kind: Stream
metadata:
  name: ticket-email
spec:
  name: TICKET_EMAIL
  subjects: ["TICKET_EMAIL.*"]
  storage: file
  retention: limits
  maxAge: 168h
  duplicateWindow: 2m
  discard: old
  replicas: 1
  preventDelete: true
