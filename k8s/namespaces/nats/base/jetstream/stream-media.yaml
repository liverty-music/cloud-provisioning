apiVersion: jetstream.nats.io/v1beta2
kind: Stream
metadata:
  name: media
spec:
  name: MEDIA
  subjects: ["MEDIA.*"]
  storage: file
  retention: limits
  maxAge: 168h
  duplicateWindow: 2m
  discard: old
  replicas: 1
  preventDelete: true
