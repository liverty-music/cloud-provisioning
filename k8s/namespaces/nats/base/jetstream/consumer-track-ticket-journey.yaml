apiVersion: jetstream.nats.io/v1beta2
kind: Consumer
metadata:
  name: track-ticket-journey
spec:
  streamName: TICKET_JOURNEY
  durableName: track-ticket-journey
  # PULL work-queue (no deliverSubject/deliverGroup) — declarative, replaces the
  # push consumer whose random _INBOX deliver subject cannot be declared (D5).
  deliverPolicy: new
  filterSubject: TICKET_JOURNEY.status_changed
  ackPolicy: explicit
  ackWait: 30s
  maxAckPending: 1000
  maxDeliver: -1
  replayPolicy: instant
  replicas: 0
  preventDelete: true
