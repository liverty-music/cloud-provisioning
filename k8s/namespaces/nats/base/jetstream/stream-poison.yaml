apiVersion: jetstream.nats.io/v1beta2
kind: Stream
metadata:
  name: poison
spec:
  name: POISON
  subjects: ["POISON.*"]
  storage: file
  retention: limits
  maxAge: 720h
  duplicateWindow: 2m
  discard: old
  replicas: 1
  preventDelete: true
