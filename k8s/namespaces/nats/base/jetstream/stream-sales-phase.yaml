apiVersion: jetstream.nats.io/v1beta2
kind: Stream
metadata:
  name: sales-phase
spec:
  name: SALES_PHASE
  subjects: ["SALES_PHASE.*"]
  storage: file
  retention: limits
  maxAge: 168h
  duplicateWindow: 2m
  discard: old
  replicas: 1
  preventDelete: true
