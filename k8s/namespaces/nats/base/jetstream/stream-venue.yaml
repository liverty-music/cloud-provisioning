apiVersion: jetstream.nats.io/v1beta2
kind: Stream
metadata:
  name: venue
spec:
  name: VENUE
  subjects: ["VENUE.*"]
  storage: file
  retention: limits
  maxAge: 168h
  duplicateWindow: 2m
  discard: old
  replicas: 1
  preventDelete: true
