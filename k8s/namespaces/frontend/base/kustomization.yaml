apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
# fan-web: the consumer-site frontend workload (unify-workload-naming),
# successor to the deleted `web` workload. Owns liverty-music.app via
# fan-web-route.
- fan-web

namespace: frontend
